import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "TeachBuddy.ai - AI-Powered Personalized Learning",
  description: "Transform your teaching materials into adaptive learning experiences. AI-powered diagnostic tests and personalized tutoring.",
  keywords: ["AI tutoring", "personalized learning", "adaptive education", "teacher tools"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans">
        {children}
      </body>
    </html>
  );
}
